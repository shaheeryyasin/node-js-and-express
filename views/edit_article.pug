extends layout 

block content 
        h1 #{title}
        form(method='POST', action='/articles/edit/'+article._id)
                #form-group 
                        label Title:
                        input.form-control(name='title', type='text', value=article.title)
                 #form-group 
                        label Author:
                        input.form-control(name='author', type='text', valye=article.author)
                 #form-group 
                        label body:
                        input.form-control(name='body') article.body
                 input.btn.btn-primary(type='submit',value='Submit')
                
                                